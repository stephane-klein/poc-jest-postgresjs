{
	"name": "poc-jest-postgresjs",
	"version": "1.0.0",
  "type": "module",
	"scripts": {
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js",
    "migrate:reset": "graphile-migrate reset --erase",
    "migrate:status": "graphile-migrate status",
    "migrate:migrate": "graphile-migrate migrate",
    "migrate:commit": "graphile-migrate commit",
    "migrate:watch": "graphile-migrate watch",
    "migrate-test:reset": "graphile-migrate --config=.gmrc-test reset --erase",
    "migrate-test:status": "graphile-migrate --config=.gmrc-test status",
    "migrate-test:migrate": "graphile-migrate --config=.gmrc-test migrate",
    "migrate-test:commit": "graphile-migrate --config=.gmrc-test commit",
    "migrate-test:watch": "graphile-migrate --config=.gmrc-test watch"
	},
	"dependencies": {
		"graphile-migrate": "1.4.1",
		"jest": "29.6.4",
		"postgres": "3.3.5"
	}
}
